Apply **Document** — structured documentation production for any artifact type.

**Use when:** writing new docs, updating existing docs, auditing documentation accuracy
**Not for:** code review → use review | code implementation → use develop

**Invocation template:**
```
Use [technical-writer | architect | python-engineer].
Apply document.
Relevant domains: documentation
Task: [subject + document type + audience]
Constraints: [length, format, depth]
```

**Workflow:** Classify → Define audience → Gather source → Declare gaps → Outline → Draft → Validate → Summarize