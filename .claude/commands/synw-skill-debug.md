Apply **Debug** — systematic diagnosis and resolution of defects.

**Use when:** bugs, errors, unexpected behavior, failures
**Not for:** new features or restructuring

**Invocation template:**
```
Use [architect | python-engineer].
Apply debug.
Relevant domains: [list]
Task: [expected behavior vs. actual behavior]
Constraints: [OS, Python version, dependencies, steps to reproduce]
```

**Workflow:** Restate → Inspect evidence → Hypotheses → Eliminate → Root cause → Fix → Validate → Summarize